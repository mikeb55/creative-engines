/**
 * Composer OS — single source of truth for app API constants (shared with desktop bundle).
 */

export const COMPOSER_OS_APP_NAME = 'Composer OS';
/** Display / product name for desktop window and UI (matches electron-builder productName). */
export const COMPOSER_OS_DESKTOP_PRODUCT_NAME = 'Composer OS';
export const COMPOSER_OS_VERSION = '1.0.0';
export const COMPOSER_OS_API_BASE_PATH = '/api';
export const COMPOSER_OS_PREFERRED_PORT_DEFAULT = 3001;
