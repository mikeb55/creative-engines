/**
 * Composer OS V2 — conductor role vocabulary (declarative / metadata only).
 * Maps conceptual pipeline responsibilities; does not reroute runtime.
 */

export type ConductorRole =
  | 'form'
  | 'feel_rhythm'
  | 'density'
  | 'register'
  | 'motif'
  | 'style'
  | 'interaction'
  | 'instrument_behaviour'
  | 'score_model'
  | 'validation'
  | 'export';
