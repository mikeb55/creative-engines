# Composer Studio Launcher

Double-click `composer_studio_launcher.bat` to run a default generation.

Outputs: `outputs/composer_studio/studio_runs/<timestamp>/`

To customize: edit the Python command in the .bat file (input_text, preset_name, seed).
