{
  "name": "creative-engines",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "npm run test --prefix engines/big-band-architecture-engine && npm run test --prefix engines/arranger-assist-engine && npm run test --prefix engines/selective-big-band-generation-engine",
    "test:fullstack": "npx ts-node --project tsconfig.json validation/full-stack/fullStackValidation.ts"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
